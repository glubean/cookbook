# Tests

This directory is for committed verification tests — the ones that run in CI on every commit.

Start by promoting useful `explore/` tests here once they're stable.

```bash
glubean run           # runs tests/ by default
glubean run --explore  # runs explore/ instead
```
